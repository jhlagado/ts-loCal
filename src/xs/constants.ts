export const DSTACK_SIZE = 100;
export const RSTACK_SIZE = 100;
export const HEAP_SIZE = 1000;

export const CELL = 4;
export const MEM_SIZE = 1000000;
export const MEM_START = 0;
export const ROM_SIZE = 100000;
export const ROM_START = 0;
export const RAM_SIZE = 100000;
export const RAM_START = ROM_START + ROM_SIZE;
export const ROM_END = RAM_START;

export const DEBUG = false;

export const NULL = 0;
export const TRUE = -1;
export const FALSE = 0;
